package com.martasd.hungermeter;

import android.os.Bundle;
import android.annotation.TargetApi;
import android.app.Fragment;
 

/** NEXT VERSION: Defines the interface through which user enters food amount. */
/** Code to add this fragment to StatusActivity */
//
///** EnterEnergyFragment is the interface through which user enters amount */
//FragmentManager fragmentManager = getFragmentManager();
//FragmentTransaction fragmentTransaction = fragmentManager.beginTransaction();
//
//EnterEnergyFragment fragment = new EnterEnergyFragment();
//fragmentTransaction.add(0, fragment);
//fragmentTransaction.commit();

@TargetApi(11)
public class EnterEnergyFragment extends Fragment {

	@Override
	public void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		
 
      	
      }
		
	}
